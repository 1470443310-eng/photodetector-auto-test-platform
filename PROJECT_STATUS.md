# 项目状态（v0.3.0 现实量级教学仿真版）

## 已完成

- 正式立项、范围、风险、阶段门和验收标准；
- 光电二极管简化模型及 8 种正常/故障配置；
- 虚拟 SMU、虚拟光源、安全互锁与 Compliance；
- 暗态 I–V、光照 I–V、响应度、重复性流程；
- PASS、FAIL、ERROR 三态判定；
- 20 DUT 批处理、良率与失败原因统计；
- CSV、JSON、SQLite、日志、HTML 报告；
- 命令行入口、Streamlit 入口、真实 VISA/SCPI 适配骨架；
- 8 项自动化测试及可复现样例数据；
- 原理、架构、电脑端落实、开源与 AI 边界、博士审阅文档。
- 暗噪声、NEP、比探测率 D*、多功率线性度、稳定性和光谱响应测试；
- 数据清洗审计、批次稳健异常检测、失败原因 Pareto 和综合 Quality Report；
- Streamlit 四页签工作台：综合判定、单颗曲线、Batch 分析与指标学习说明；中文质量报告在运行后直接显示于主页面。
- 网站右上角常驻“查看说明”入口，可直接阅读现实仿真与学生指南；页面显示本次/上次种子供用户核对随机变化。
- 反馈存储支持本机 SQLite 与云端 PostgreSQL 自动切换，提供按反馈编号去重的迁移脚本；管理员密码与数据库连接串支持 Streamlit Cloud Secrets。
- 匿名用户反馈表、SQLite 持久化、管理员密码保护、反馈统计和 JSON 导出；
- Cloudflare Quick Tunnel 公网测试部署及外网 HTTP/健康接口验证。
- Streamlit 默认现实波动模式，每次生成新批次种子；保留固定种子教学复现模式；
- 器件制造离散、温度、仪器增益/零点/噪声模型和可追溯仿真参数；
- 全中文状态、故障归因、曲线坐标、指标学习说明和学生排障建议；
- 通用硅 PIN 光电二极管参数量级参考 Vishay BPW34 与 Hamamatsu 技术资料。

## 已验证

- Python 3.12.14 下核心依赖安装成功；
- `python -m pytest`：19 passed；
- 20 DUT 基线：PASS 13、FAIL 6、ERROR 1；
- 样例产物完整写入 `sample_data/`。

## 下一阶段

1. 在用户电脑重新建环境并复现相同基线。
2. 用户本人修改阈值、制造故障、定位并记录 Git commit。
3. 选定真实光电二极管、SMU 和光源型号。
4. 根据 datasheet 与仪器编程手册替换阈值和 SCPI 命令。
5. 增加真实数据的校准、环境条件和不确定度说明。

## 未验证边界

Streamlit 界面已在本机 Python 3.12.14 / Streamlit 1.43.2 环境完成自动页面验收；真实仪器接口仍未做硬件在环验证，器件规格、带宽、面积和光谱模型均为 Demo 假设值。后两者不能在展示中描述为真实实验验证。
